#version 330 core

in float Color;
in vec2 TexCoord;
out vec4 FragColor;

uniform sampler2D blockTexture;

void main(){
	vec4 TexturePixel = texture(blockTexture, TexCoord) * Color;
	FragColor = vec4(TexturePixel.x, TexturePixel.y, TexturePixel.z, 1.0);
}