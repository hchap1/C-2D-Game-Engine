#version 330 core

in float Color;
in vec2 TexCoord;
out vec4 FragColor;

uniform sampler2D blockTexture;

void main(){
	vec4 TexturePixel = texture(blockTexture, TexCoord);
	FragColor = vec4(TexturePixel.x * Color, TexturePixel.y * Color, TexturePixel.z * Color, TexturePixel.w * (Color + 0.3));
}